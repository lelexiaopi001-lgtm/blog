# Aurora Blog 极光博客 · 开发者文档

> 本文件供接手本项目的开发者 / AI 快速了解架构、修改代码、重新部署。
> 技术栈：原生 PHP 8 + MySQL 5.7/8.0，零第三方依赖，无需 Composer。

---

## 一、项目概览

| 项目 | 说明 |
|------|------|
| 名称 | Aurora Blog 极光博客 |
| 技术栈 | 原生 PHP 8（OOP）、MySQL（PDO 预处理）、原生 HTML/CSS/JS |
| 部署环境 | Ubuntu + 宝塔面板（LNMP），Nginx 伪静态 |
| 站点根目录 | `public/`（宝塔站点指向此目录） |
| 后台入口 | `/admin/index.php`（路由参数 `?r=xxx`） |
| 生产地址 | http://leyan20.me / http://218.244.142.144 |
| 服务器 | 阿里云 ECS 218.244.142.144（宝塔已装 PHP 8.2） |
| 数据库 | MySQL（库名见 `config/config.php`，安装时自定义） |

---

## 二、目录结构

```
aurora-blog/
├── public/                    # ⭐ Web 根目录（唯一对外暴露）
│   ├── index.php              # 前台入口（所有前台请求都走它）
│   ├── install.php            # 安装向导入口（require tools/install.php）
│   ├── admin/
│   │   └── index.php          # 后台入口
│   ├── assets/
│   │   ├── css/main.css       # 前台样式（暗/亮双主题）
│   │   ├── css/admin.css      # 后台样式
│   │   ├── js/main.js         # 前台脚本（主题/评论/点赞/复制/TOC）
│   │   └── js/admin.js        # 后台脚本（图表/编辑器/上传/媒体库）
│   ├── uploads/               # 图片上传目录（777，含 .gitkeep 占位）
│   └── .htaccess              # Apache 备用伪静态
├── app/
│   ├── core/
│   │   ├── Core.php           # 核心框架：DB/Helper/Auth/View/App 全部类
│   │   └── Markdown.php       # 自研安全 Markdown 解析器
│   ├── controllers/
│   │   └── FrontController.php# 前台控制器（全部页面方法）
│   ├── admin/
│   │   ├── AdminController.php# 后台控制器（全部管理方法）
│   │   └── views/             # 后台视图（12 个模板）
│   └── views/                 # 前台视图（layout/home/post/list/about）
├── config/
│   ├── config.php             # 实际配置（安装向导生成，含数据库密码）
│   └── config.sample.php      # 配置模板
├── database/blog.sql          # 建库脚本 + 默认数据
├── tools/install.php          # 安装向导（装完需删除）
├── docs/
│   ├── 宝塔部署傻瓜教程.md      # 给站长的部署指南
│   └── 开发者文档.md           # 本文件
├── nginx.conf                 # 宝塔伪静态规则
└── README.md
```

---

## 三、核心架构

### 1. 入口与路由

**前台** `public/index.php` → 加载 `app/core/Core.php` → `App::run()`：

```
路径解析：parse_url(REQUEST_URI) → explode('/') → urldecode 每段（支持中文 slug）
规则：
  /                  → FrontController::home(page)
  /page/{n}          → home 分页
  /post/{slug}       → post 详情
  /category/{slug}[/page/{n}] → 分类列表
  /tag/{slug}[/page/{n}]      → 标签列表
  /archive[/page/{n}] → 归档
  /search?q=xx       → 搜索
  /about             → 关于页（含友链）
  /rss.xml           → RSS
  /api/{action}      → 轻量 API（like/comment，POST + CSRF）
  其他               → 404
```

**后台** `public/admin/index.php` → `AdminController::dispatch($r)`：

```
?r=login|logout|dashboard|posts|post_edit|post_save|post_delete|post_restore|post_purge|
  categories|category_save|category_delete|tags|tag_save|tag_delete|
  comments|comment_approve|comment_reject|comment_delete|
  links|link_save|link_delete|media|media_delete|settings|settings_save|
  stats|profile|profile_save|upload
```

### 2. 核心类（均在 `app/core/Core.php`）

| 类 | 职责 |
|----|------|
| `DB` | PDO 单例；`q/fetch/all/one/insert/update/delete` 全部参数化查询，防 SQL 注入 |
| `Helper` | `e()` HTML 转义（防 XSS）、`csrfToken/csrfCheck`、`setting()` 设置缓存读取、`slugify`、`upload`（白名单）、`paginate/pagination`、`recordVisit`（PV/UV）、`abort` |
| `Auth` | Session + Cookie「记住我」；`check/requireLogin/login/logout`；登录失败 5 次锁 15 分钟 |
| `View` | 模板渲染：`render(tpl, data, layout)` —— 先执行内容模板（可设 `$GLOBALS['__pageTitle']` 等），缓冲输出，再渲染 layout |
| `App` | 前台路由分发（见上） |
| `Markdown` | 自研解析器：先转义再白名单输出；支持标题/粗体/代码块/表格/引用/列表/任务列表/链接/图片 |

### 3. 模板渲染机制（重点）

```
View::render('home', $data) 执行流程：
1. extract($data) 使模板内可直接用 $posts 等变量
2. 先 require 内容模板（home.php）→ 缓冲其 HTML，可设置页面变量
3. 再 require layout.php → layout 里 View::content() 输出缓冲内容
```

- 前台 layout：`app/views/layout.php`（导航/侧边栏/页脚）
- 页面级变量通过 `$GLOBALS` 传递：`__pageTitle`（浏览器标题）、`__pageDesc`、`__nav`（高亮导航）
- 侧边栏数据：`FrontController::side()` 写入 `$GLOBALS['side']`
- 后台 layout：`app/admin/views/layout.php`，`$GLOBALS['__adminTpl']` 指定内容模板

### 4. 安全机制（已内置，勿删除）

| 威胁 | 防护 |
|------|------|
| SQL 注入 | 全站 PDO 预处理（`DB::q` 参数绑定） |
| XSS | 所有输出用 `Helper::e()`；Markdown 先转义 |
| CSRF | 表单 `Helper::csrfField()` + 后台 POST 统一 `csrfCheck()` |
| 密码 | `password_hash`(bcrypt) + 登录限速 |
| 上传 | 类型白名单(jpg/png/gif/webp) + 随机文件名 + 5MB 限制 + realpath 校验删除 |

---

## 四、数据库设计

```
users         管理员（username/password_hash/email/nickname/avatar/bio/role/remember_token/last_login）
categories    分类（name/slug/description/sort/post_count）
tags          标签（name/slug/post_count）
posts         文章（title/slug/summary/content/cover/category_id/user_id/status[draft|published|trash]/is_top/views/likes/comment_status/comment_count/published_at）
post_tags     文章-标签 关联（post_id+tag_id 复合主键）
comments      评论（post_id/parent_id/nickname/email/website/content/status[pending|approved|rejected]/ip）
links         友链（name/url/logo/description/sort/status）
settings      站点设置（key-value 表，skey 主键）
visits        访问统计（vdate 唯一，pv/uv 按天聚合）
login_attempts 登录失败记录（防爆破）
```

**settings 已有键**：site_name、site_desc、site_slogan、site_motto、site_hero_eyebrow、site_keywords、site_icp、site_logo、site_avatar、site_about、site_social_*、comment_switch、comment_audit、page_size、theme_mode、footer_text

**添加新设置项**：改 `AdminController::settingsSave()` 的 `$fields` 数组 + `settings.php` 表单 + `blog.sql` 默认值（三处同步）。

---

## 五、前台页面 → 文件映射

| 页面 | 控制器方法 | 视图 |
|------|-----------|------|
| 首页 | `FrontController::home()` | `views/home.php` |
| 文章详情 | `FrontController::post($slug)` | `views/post.php` |
| 分类/标签/归档/搜索 | `category/tag/archive/search()` | `views/list.php` |
| 关于页 | `about()` | `views/about.php` |
| RSS | `rss()` | 直接输出 XML |
| 全局布局 | - | `views/layout.php` |

---

## 六、后台页面 → 文件映射

| 路由 r= | 控制器方法 | 视图 |
|--------|-----------|------|
| dashboard | `dashboard()` | `views/dashboard.php` |
| posts | `posts()` | `views/posts.php` |
| post_edit | `postEdit()` | `views/post-edit.php`（Markdown 编辑器 + 预览） |
| categories / tags | `categories()` / `tags()` | `views/categories.php` / `views/tags.php` |
| comments | `comments()` | `views/comments.php` |
| links | `links()` | `views/links.php` |
| media | `media()` | `views/media.php`（媒体库） |
| settings | `settings()` | `views/settings.php` |
| stats | `stats()` | `views/stats.php`（纯 SVG 图表） |
| profile | `profile()` | `views/profile.php` |
| upload | `upload()` | AJAX 返回 JSON |

---

## 七、常见修改任务

### 1. 改首页视觉（Hero 区）
文件：`app/views/home.php` + `public/assets/css/main.css`（`.hero-*` 样式）
Hero 文案全部来自 settings：欢迎语 / 站点名 / slogan / motto。

### 2. 新增一个设置项（如"公告"）
1. `AdminController::settingsSave()` 的 `$fields` 加 `'site_notice'`
2. `app/admin/views/settings.php` 加输入框（name="site_notice"）
3. 需要展示处用 `Helper::setting('site_notice')`
4. `database/blog.sql` 默认数据补一行

### 3. 新增后台菜单
`app/admin/views/layout.php` 的 `$nav` 数组加一项 → `AdminController::dispatch()` 注册路由 + 方法 → 新增视图文件。

### 4. 新增前台页面/路由
`App::run()` 的 switch 加 case → `FrontController` 加方法 → `app/views/` 新增模板。

### 5. 修改主题配色
`public/assets/css/main.css` 顶部 `:root` 变量（`--bg/--text/--accent/--grad` 等），暗色默认，`[data-theme="light"]` 覆盖亮色。

### 6. 图表
`admin.js` 的 `renderChart()` 纯 SVG 实现，零依赖；`stats.php`/`dashboard.php` 通过 `data-*` 属性传数据。

---

## 八、部署与更新流程（重要！）

### 打包前权限设置（防 Access denied 的根因！）
zip 会保存 Unix 权限位，**必须先把文件权限调好再打包**，否则解压到宝塔后 www 用户读不了：

```bash
cd /workspace/aurora-blog
find . -type d -exec chmod 755 {} \;
find . -type f -exec chmod 644 {} \;
chmod 777 public/uploads config        # uploads 需可写；config 安装时需写
chmod 755 tools/install.php public/install.php
cd /workspace
rm -f blog.zip
zip -r -9 blog.zip aurora-blog -x "aurora-blog/.git/*"
```

### 服务器更新步骤
1. 宝塔 → 文件 → `/www/wwwroot` → 上传 `blog.zip` → 解压（选覆盖）
2. 无需跑 chmod/chown（权限已内置；如仍异常可补：`chown -R www:www /www/wwwroot/aurora-blog`）
3. 浏览器 **Ctrl+F5** 强制刷新（CSS/JS 有浏览器缓存）

### 宝塔注意事项
- 站点根目录：`/www/wwwroot/aurora-blog/public`
- 伪静态：`try_files $uri $uri/ /index.php?$query_string;`
- 必须关闭「防跨站攻击(open_basedir)」（代码在 public 外）
- PHP 版本：8.x（当前 8.2）；fileinfo 非必需

---

## 九、已知注意事项 / 坑

1. **uploads 目录**：zip 不打包空目录，已放 `.gitkeep` 占位保证解压存在
2. **中文 slug**：路由已 urldecode，分类/文章标题可为中文，链接自动编码
3. **浏览器缓存**：改 CSS/JS 后用户需 Ctrl+F5；长期方案可给资源加版本号 `?v=`
4. **登录 403 令牌**：CSRF 会话过期时清 Cookie 重登
5. **评论**：默认需审核（comment_audit=1），后台评论管理里通过
6. **访问统计**：按天聚合写 visits 表，统计页支持 7/30/90 天
7. **安装向导**：部署后务必删除 `public/install.php` 与 `tools/` 目录
8. **调试**：`config/config.php` 中 `'debug' => true` 可显示详细错误，生产应关

---

## 十、改进方向（待办）

- [ ] 资源版本号（`?v=`）避免缓存问题
- [ ] SSL 证书确认（Let's Encrypt 是否已申请）
- [ ] 评论邮件通知（管理员收件提醒）
- [ ] PWA / 离线缓存
- [ ] 文章字数 / 阅读时长统计
- [ ] 更多 Markdown 语法（脚注、TOC 生成）
- [ ] 后台暗色主题增强 / 自定义配色
